[package]
name = "stt-pulse"
version = "0.1.0"
edition = "2024"

[dependencies]
audio = { version = "0.1.0", path = "../audio" }
dotenvy = "0.15.7"
reqwest = { version = "0.13.4", features = ["rustls", "json"] }
serde = { version = "1.0.229", features = ["derive"] }
tinyvox-engine = { version = "0.1.0", path = "../engine" }
tokio = { version = "1.53.1", features = ["rt-multi-thread"] }
url = "2.5.7"
